import { actions } from './actions.js';
import mutations from './mutations.js';
const state = {
  certificateList: [],
  pageSum:0
};

export default {
  actions,
  mutations,
  state,
};
